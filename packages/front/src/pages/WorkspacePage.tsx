import React from 'react';
import { Route } from 'react-router-dom';

const WorkspacePage: React.FC = () => {
  return <Route path="workspace" element={<div>workspace</div>} />;
};

export default WorkspacePage;
